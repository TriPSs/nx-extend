{
  "name": "actions-run-many",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/core/src",
  "projectType": "library",
  "targets": {
    "compile": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx esbuild ./actions/run-many/src/run-many.ts --bundle --platform=node --target=node24 --format=cjs --outfile=./actions/run-many/dist/index.js"
        ]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "compile"
      ],
      "options": {
        "commands": [
          "git add ./actions/run-many/dist -f || true",
          "git commit -m 'chore(actions-run-many): Add dist' || true"
        ],
        "parallel": false
      }
    }
  },
  "tags": []
}
