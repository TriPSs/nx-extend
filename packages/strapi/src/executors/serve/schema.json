{
  "version": 2,
  "outputCapture": "direct-nodejs",
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "title": "Serve executor",
  "description": "Runs the command 'strapi develop'",
  "properties": {
    "production": {
      "type": "boolean",
      "description": "Build Strapi in production mode",
      "$default": false
    }
  },
  "required": []
}
