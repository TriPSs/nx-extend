{
  "title": "Deploy executor",
  "description": "Deploy",
  "type": "object",
  "properties": {
    "file": {
      "type": "string",
      "description": "What file to deploy"
    },
    "project": {
      "type": "string",
      "description": "Project id to deploy to"
    },
    "deletePolicy": {
      "type": "string"
    },
    "createPolicy": {
      "type": "string"
    }
  }
}
