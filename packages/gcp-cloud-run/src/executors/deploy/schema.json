{
  "title": "Deploy executor",
  "description": "Deploy",
  "type": "object",
  "properties": {
    "region": {
      "type": "string",
      "description": "Region to deploy to"
    },
    "project": {
      "type": "string",
      "description": "Project id to deploy to"
    }
  }
}
