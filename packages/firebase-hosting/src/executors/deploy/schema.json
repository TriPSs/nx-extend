{
  "version": 2,
  "outputCapture": "direct-nodejs",
  "$schema": "https://json-schema.org/schema",
  "type": "object",
  "title": "Deploy executor",
  "description": "Deploy to Firebase Hosting",
  "properties": {
    "site": {
      "type": "string",
      "description": "Site name in Firebase Hosting"
    },
    "identifier": {
      "type": "string",
      "description": "Identifier name in Firebase Hosting"
    },
    "cliVersion": {
      "type": "string",
      "description": "Specific version of the Firebase CLI to use, left empty will use the latest"
    }
  }
}
